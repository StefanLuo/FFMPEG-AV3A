AVS3A (Audio vivid)编解码库集成分析
====

## 1. 项目说明
* avs2p3Encoder: 无损音频编码器和通用高速率音频编码器
* avs2p3Decoder: 无损音频解码器和通用高速率音频解码器
* avs3Encoder: 通用全速率音频编码器
* avs3Decoder: 通用全速率音频解码器

## 2. 知识背景
* 什么是三维声
    * 三维声相对传统声音增加了空间感和方位感。
* 如何重现声音的空间感和方位感
    1. 基于声道的实现方式
        * 对声道进行配置，将每个声道的音源映射到指定扬声器。优点是实现简单，缺点是扬声器布局必须固定；一种扬声器布局对应一种布置。
    2. 基于声床+对象的实现方式
        * 声床承载基本环境声
        * 对象是一系列单声道音频元素和对应元数据的集合。元数据表示对象的位置、强度、大小等信息。
        * 回放时，根据元数据信息，将对象映射到一个或多个扬声器，或双耳渲染到耳机播放，以达到想要的空间音频效果。
    3. 基于声场的实现方式
        * 基于对空间点多个声波场的压力函数建模，获取相应系数，以实现对空间中声音的重构。
        * Higher Order Ambisonics(HOA) 是一种定义在球体表面上的3D声场建模格式，可以在任何设备(如耳机、扬声器、音箱)上对声场实现准确捕获、处理和重构。
        * HOA 系统性能随着HOA阶数的增加而增加，但HOA信号数量也随之而增加，因此需要更强的信号处理能力及AI技术的引入。
* Audio vivid算法原理
    * 多声道编码技术去除多声道信号间的信息冗余.
    * HOA编码技术去除HOA各声道间的空间几何信息冗余.
    * 基于心理声学模型的预处理和基于AI的量化，熵编码技术去除单声道、对象音频信号中的信息冗余。
    * 通过扬声器和耳机完成最终渲染。
* 通用全码率音频编码工具
    * 采用神经网络变换、量化和熵编码技术
    * 基于声道相关性的多声道下混和比特分配技术
    * 基于虚拟扬声器的HOA空间编码技术
    * 适用于单声道、立体声、多声道编码、对象音频编码、混合音频编码、HOA编码。
    * 采样率支持32KHz~192KHz，支持16比特和24比特采样精度。支持编码输出位流为：
        1. 单声道/对象：32、44、56、64、72、80、96、128、144、164、192kb/s;
        2. 立体声: 32、48、64、80、96、128、144、192、256、320kb/s;
        1. 5.1 多声道：96、128、144、160、192、256、320、384、448、512、640、720kb/s;
        1. 7.1 多声道：128、160、192、256、384、480、576、640kb/s;
        1. 5.1.4 三维声：176、256、384、448、576、704kb/s;
        1. 7.1.4 三维声：240、384、512、608、832kb/s;
        1. FOA：96、128、192、256kb/s;
        1. 2 阶 HOA：192、256、320、384、480、512、640kb/s;
        1. 3 阶 HOA：256、320、384、512、640、896kb/s;
        1. 声床+对象混合信号：以上立体声/多声道/三维声信号和单声道/对象信号码率的各种组合;
    * 典型信号推荐码率
        1. 5.1：不小于 192kbps，针对极高音质应用不小于 320kps
        2. 5.1.2：不小于 320kbps，针对极高音质应用不小于 480kbps
        1. 5.1.4：不小于 384kbps，针对极高音质应用不小于 576kbps
        1. 7.1.4：不小于 384kbps，针对极高音质应用不小于 608kbps
        1. 每个对象：不小于 96kbps，针对极高音质应用不小于 164kbp

## 3. 通用全码率编解码器参数说明

### 2.1 编码结构体对象：AVS3EncoderHandle(为AVS3Encoder对应指针类型)主要参数说明

|  参数名   | 类型  | 说明 | 取值范围 |
|  ----  | ----  |  ----  |  ----  |
| bwidth  | short | 最大编码带宽 |  SWB(0)：超宽带<br> FB(1)：全带 | 
| bitDepth| short | 位深 | 16 或 24 位深 |
| numChansInput| short | 输入声道数 |  |
| avs3CodecFormat | short | 编码模式 | AVS3_MONO_FORMAT(0)：单声道模式 <br> AVS3_STEREO_FORMAT(1): 立体声模式 <br> AVS3_MC_FORMAT(2): 多声道模式 <br> AVS3_HOA_FORMAT(3): HOA模式 <br> AVS3_MIX_FORMAT(4): 混合信号模式 |
| avs3CodecCore | short | 编码内核 | AVS3_MDCT_CORE(0): 目前就定义了这一种 |
| channelNumConfig | short | 多声道扬声器配置 | 参考注①，各个宏定义 |
| soundBedType | short | 声床类型 | 0: 不包含声床 , 纯对象 <br> 1: 表示多声道声床 + 对象信号 |
| numObjsInput | short | 对象信号数量 | |
| isMixedContent | short | 混合内容标记 | 1 表示包含混合内容 |
| bitratePerObj | long | 每个对象的编码速率 | |
| bitrateBedMc | long | 声床信号编码速率 | |
| totalBitrate | long | 总码率 | |
| hasLfe | short | 是否支持 LFE | 声床类型为纯对象、立体声、或MC_4_0 不支持(0)。其它情况为1 |
| nn_type | NnTypeConfig | 神经网络配置 | 0: 基本配置 <br> 1: 低复杂度配置 |
| inputFs | long | 采样率 | |
| frameLength | short | 帧大小 | 目前仅支持1024 |
| enableSilDetect | short | 启用静音检测 | 0: 不器用, 1: 启用 |
| silThrehold | float | 静音检测阈值 (db) | ≤ -65 db|
||
* 注①：多声道扬声器配置宏定义
    * CHANNEL_CONFIG_MONO = 0, 
    * CHANNEL_CONFIG_STEREO = 1, 
    * CHANNEL_CONFIG_MC_5_1,
    * CHANNEL_CONFIG_MC_7_1,
    * CHANNEL_CONFIG_MC_10_2,
    * CHANNEL_CONFIG_MC_22_2,
    * CHANNEL_CONFIG_MC_4_0,
    * CHANNEL_CONFIG_MC_5_1_2,
    * CHANNEL_CONFIG_MC_5_1_4,
    * CHANNEL_CONFIG_MC_7_1_2,
    * CHANNEL_CONFIG_MC_7_1_4,
    * CHANNEL_CONFIG_HOA_ORDER1,
    * CHANNEL_CONFIG_HOA_ORDER2,
    * CHANNEL_CONFIG_HOA_ORDER3,
    * CHANNEL_CONFIG_UNKNOWN
* 注②：LFE: low frequency effect, 低频效果。和低音管理(Bass management)，下混合(downmix)，低音扬声器(Subwoofer 俗称低音炮)等概念一样，是录音棚工作时必然面对的技术。

### 2.2 解码结构体对象主要参数说明(AVS3DecoderHandle)

|  参数名   | 类型  | 说明 | 取值范围 |
|  ----  | ----  |  ----  |  ----  |
| codec_id | short 
| avs3CodecFormat | short | codec 格式 | @见编码器同名字段 |
| avs3CodecCore | short | codec 内核 | AVS3_MDCT_CORE(0): 目前就定义了这一种 |
| numChansOutput | short | 解码输出的声道数 | |
| outputFs | long | 采样率 | |
| frameLength | short | 帧大小 | 当前仅支持 1024 |

## 4. 编解码参考软件相关信息

### 4.1 函数语义

* Avs3MetadataDec()             元数据解码
* Avs3MonoDec()                 单声道解码
* Avs3StereoDec()               双声道立体声解码
* Avs3McDec()                   多声道解码
* Avs3HoaDec()                  HOA解码
* DecodeCoreSideBits()          解析核心解码器边信息
* DecodeGroupBits()             频谱逆分组处理
* DecodeStereoSideBits()        解析双声道立体声边信息
* DecodeMcSideBits()            解析多声道边信息
* DecodeHoaSideBits             解析HOA边信息
* StereoBitsAllocation()        双声道立体声比特分配
* McBitsAllocation()            多声道比特分配
* HoaSplitBytesGroup()          HOA比特分配
* StereoInvMsProcess()          双声道立体声上混处理
* Avs3McacDec()                 多声道解码处理
* Avs3HoaInverseDMX()           HOA解码处理
* DecodeQcBits()                解析量化编码边信息
* Avs3InverseQC()               区间解码和逆量化
* Avs3PostSynthesis()           解码后处理
* HoaPostSynthesisFilter()      HOA空间解码
* codecFormat                   表示解码模式。根据附录A中表A.8 声道数索引(channel_number_index)决定，<br>
                                当声道配置为单声道时，codecFormat 为0， 配置为双声道时，codecFormat 为1， 多声道时，codecFormat 为2                                
* numChans                      音频信号的声道数，当coding_profile 为0时，复用附录A中表A.8声道数索引中的channel_number.

### 4.2 解码过程

通用全码率音频编码原始位流数据(ga_co_raw_data_block)是构成AASF和AATF编码位流的基本单位，解码ga_co_raw_data_block可获得一帧音频样本，解码帧头可以获得当前帧比特率。

## 5. AASF 和 AATF 语法及语义

### 5.1 说明

    三维声音频参考 GB/T 33475.3-2018中AASF和AATF规范，给出三维声音频存储格式(AASF)和传输格式(AATF)语法及语义描述。三维声音频新增通用全码率编码工具定义audio_codec_id为2，三维声音频新增通用全码率音频编码工具语义复用GB/T 33476.3 中AASF和AATF规范。
* AASF包含音频序列的头信息及随后的原始数据块。AASF仅适用于定义了起始点而无需从音频数据流中间开始解码的系统。AASF包含所有解码和播放音频数据必须信息，是一种交换存储格式。
* AATF包含同步字和解码必须得信息，是一种流传输格式。同步字似的解码器无需确定的起始点即可进行解码。

### 5.2 AASF语法

AASF包含音频序列的头信息及随后的原始数据块。AASF语法如下
```
aasf_sequence()
{
    aasf_header();
    if (audio_codec_id < 2) {
        if (coding_profile == 0) {
            if (audio_codec_id == 1)
                ll_raw_data_stream()
        }
        if (coding_profile == 2)
            hoa_raw_data_stream()
    } else if (audio_codec_id == 2) 
        ga_co_raw_data_stream()
}
```

AASF头描述了音频存储格式的头信息，包括AASF头数据存储格式标识、大小等。aasf_header()语法:
```
aasf_header()
{
    aasf_id             // 32bits
    header_size         // 24bits
    raw_stream_length
}
```